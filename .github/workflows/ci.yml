name: CI - Build & Test

on:
  push:
    branches: [develop, main]
  pull_request:
    branches: [develop, main]
  workflow_call:

env:
  DOTNET_VERSION: '9.0.x'
  SOLUTION_PATH: 'ES.ConexaoSolidaria.Usuarios.slnx'
  IMAGE_NAME: 'conexao-solidaria-usuarios:test'

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Configurar .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Cache de pacotes NuGet
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      - name: Restaurar dependências
        run: dotnet restore ${{ env.SOLUTION_PATH }}

      - name: Build
        run: dotnet build ${{ env.SOLUTION_PATH }} --no-restore --configuration Release

      # 1. Executa os testes exatamente como no seu arquivo original
      - name: Testes unitários
        run: dotnet test ${{ env.SOLUTION_PATH }} --no-build --configuration Release --verbosity normal --logger "trx;LogFileName=test-results.trx" --collect:"XPlat Code Coverage"

      # 2. SALVA OS ARTEFATOS IMEDIATAMENTE (Antes de mexer com Docker)
      - name: Publicar resultados de teste
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: '**/test-results.trx'

      - name: Publicar cobertura de código
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: '**/coverage.cobertura.xml'

      # 3. Agora que os testes estão salvos e garantidos, lidamos com a imagem e a segurança
      - name: Build da imagem Docker local
        run: |
          docker build -t ${{ env.IMAGE_NAME }} -f src/NomeDoProjetoDaSuaAPI/Dockerfile .

      - name: Escaneamento de vulnerabilidades (Trivy)
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ env.IMAGE_NAME }}
          format: 'template'
          template: '@/contrib/html.tpl'
          output: 'trivy-report.html'
          severity: 'HIGH,CRITICAL'
          exit-code: '0'

      - name: Publicar relatório de segurança
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: security-report
          path: 'trivy-report.html'
